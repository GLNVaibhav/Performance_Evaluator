[pytest]
pythonpath = .
# Domain models are named TestPlan/TestResult/TestRun/TestType -- disable
# pytest's default "class Test*" collection heuristic so it doesn't warn
# trying (and failing) to collect them as test classes.
python_classes =
